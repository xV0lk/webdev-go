{{define "header"}}
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Home</title>
  </head>
  <body class="bg-slate-900 text-slate-200">
    <header class="bg-slate-300 text-black">
      <nav class="px-8 py-4 flex gap-40 items-center">
        <div>🌋</div>
        <div class="flex gap-10 text-lg font-semibold text-center flex-grow">
          <a class="hover:text-teal-700" href="/">Home</a>
          <a class="hover:text-teal-700" href="/faq">Faq</a>
          <a class="hover:text-teal-700" href="/contact">Contact</a>
        </div>
        <div class="flex items-center gap-10 text-lg font-semibold text-center">
          <a class="hover:text-teal-700" href="#">SignIn</a>
          <a
            class="hover:bg-teal-600 transition-colors bg-teal-700 text-slate-200 px-3 py-1 rounded"
            href="#"
            >SignUp</a
          >
        </div>
      </nav>
    </header>
    {{ end }}

    {{define "footer"}}
  </body>
</html>
{{ end }}
